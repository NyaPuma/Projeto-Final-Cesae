# resources/js/utils

Shared utility functions.

| File | Purpose |
|------|---------|
| `api.js` | API helpers (auth headers, CSRF token) |
| `locale.js` | Localized formatting (numbers, currency, dates, unit conversion) via Intl API |
