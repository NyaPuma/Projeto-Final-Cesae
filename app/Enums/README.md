# app/Enums

Enumeration classes (backed string enums) representing standard domain state machines, role hierarchies, event types, priorities, and workflow categories across the SGM application.

## Files & Enums

| File / Enum | What it Represents | Cases & Backing Values | Key Methods |
|---|---|---|---|
| [`AuditEventEnum.php`](file:///home/nyapuma/Transferências/Projeto-Final-Cesae/app/Enums/AuditEventEnum.php) | Audit trail event actions | `Created` ('created'), `Updated` ('updated'), `Deleted` ('deleted'), `Login` ('login'), `Logout` ('logout'), `PasswordChanged` ('password_changed') | `label()`, `color()`, `isAuthEvent()`, `normalize()` |
| [`BudgetDecisionEnum.php`](file:///home/nyapuma/Transferências/Projeto-Final-Cesae/app/Enums/BudgetDecisionEnum.php) | Action decisions on repair budgets | `Approve` ('approve'), `Reject` ('reject') | `label()`, `isFinal()` |
| [`BudgetStatusEnum.php`](file:///home/nyapuma/Transferências/Projeto-Final-Cesae/app/Enums/BudgetStatusEnum.php) | Lifecycle states for ticket budget approvals | `Pending` ('pending'), `Approved` ('approved'), `Rejected` ('rejected') | `label()`, `color()`, `icon()`, `isFinal()`, `canTransitionTo()`, `normalize()` |
| [`FileTypeEnum.php`](file:///home/nyapuma/Transferências/Projeto-Final-Cesae/app/Enums/FileTypeEnum.php) | MIME/media classifications for ticket attachments | `Image` ('image'), `Document` ('document'), `Video` ('video'), `Audio` ('audio'), `Other` ('other') | `fromMimeType()`, `label()`, `icon()`, `normalize()` |
| [`MaintenancePlanIntervalTypeEnum.php`](file:///home/nyapuma/Transferências/Projeto-Final-Cesae/app/Enums/MaintenancePlanIntervalTypeEnum.php) | Recurrence interval units for preventive maintenance | `Days` ('days'), `UsageHours` ('usage_hours'), `Cycles` ('cycles') | `label()`, `normalize()` |
| [`NotificationPriorityEnum.php`](file:///home/nyapuma/Transferências/Projeto-Final-Cesae/app/Enums/NotificationPriorityEnum.php) | Urgency ranking for system and email notifications | `Low` ('low'), `Normal` ('normal'), `High` ('high'), `Urgent` ('urgent') | `label()`, `color()`, `icon()`, `weight()`, `isHighPriority()`, `normalize()` |
| [`NotificationTypeEnum.php`](file:///home/nyapuma/Transferências/Projeto-Final-Cesae/app/Enums/NotificationTypeEnum.php) | Channel event triggers for user notifications | `BudgetRequest`, `BudgetSubmitted`, `BudgetApproved`, `BudgetRejected`, `BudgetAutoApproved`, `TicketClosed`, `TicketCreated`, `PriorityOverride`, `LowStock` | `label()`, `icon()`, `color()`, `defaultPriority()`, `isBudgetRelated()`, `normalize()` |
| [`PartUnitOfMeasureEnum.php`](file:///home/nyapuma/Transferências/Projeto-Final-Cesae/app/Enums/PartUnitOfMeasureEnum.php) | Stock item quantity measurement units | `Unit` ('unit'), `Meter` ('meter'), `Liter` ('liter'), `Kg` ('kg'), `Pair` ('pair'), `Set` ('set'), `Roll` ('roll'), `Other` ('other') | `label()`, `normalize()` |
| [`PublicTicketProblemTypeEnum.php`](file:///home/nyapuma/Transferências/Projeto-Final-Cesae/app/Enums/PublicTicketProblemTypeEnum.php) | Incident category reported via public QR code form | `Breakdown` ('avaria'), `Preventive` ('manutencao_preventiva'), `Consumables` ('falta_consumiveis'), `Other` ('outro') | `label()`, `priority()`, `icon()`, `normalize()` |
| [`StockMovementTypeEnum.php`](file:///home/nyapuma/Transferências/Projeto-Final-Cesae/app/Enums/StockMovementTypeEnum.php) | Stock inventory transaction directions | `In` ('in'), `Out` ('out'), `Adjust` ('adjust'), `Return` ('return') | `label()`, `color()`, `icon()`, `normalize()` |
| [`TicketPriorityEnum.php`](file:///home/nyapuma/Transferências/Projeto-Final-Cesae/app/Enums/TicketPriorityEnum.php) | Ticket resolution urgency levels and SLA limits | `Low` ('baixa'), `Medium` ('média'), `High` ('alta'), `Critical` ('crítica') | `label()`, `color()`, `icon()`, `weight()`, `slaHours()`, `requiresImmediateAttention()`, `normalize()` |
| [`TicketStatusEnum.php`](file:///home/nyapuma/Transferências/Projeto-Final-Cesae/app/Enums/TicketStatusEnum.php) | Ticket operational statuses mapped to database lookups | `Open` ('aberta'), `InProgress` ('em curso'), `Closed` ('fechada'), `Cancelled` ('cancelada'), `PendingBudget` ('pendente orçamento'), `Rejected` ('recusada') | `label()`, `color()`, `icon()`, `isFinal()`, `isActive()`, `normalize()` |
| [`TicketWorkflowStatusEnum.php`](file:///home/nyapuma/Transferências/Projeto-Final-Cesae/app/Enums/TicketWorkflowStatusEnum.php) | Explicit workflow lifecycle and transition constraints | `Open` ('open'), `InProgress` ('in_progress'), `WaitingBudget` ('waiting_budget'), `Approved` ('approved'), `Rejected` ('rejected'), `Closed` ('closed'), `Cancelled` ('cancelled') | `label()`, `color()`, `icon()`, `isFinal()`, `isActive()`, `allowedTransitions()`, `canTransitionTo()`, `normalize()` |
| [`UserRoleEnum.php`](file:///home/nyapuma/Transferências/Projeto-Final-Cesae/app/Enums/UserRoleEnum.php) | RBAC hierarchy roles for application authorization | `User` ('user'), `Technician` ('technician'), `Admin` ('admin') | `label()`, `color()`, `icon()`, `weight()`, `isAdmin()`, `isTechnician()`, `isUser()`, `hasElevatedPrivileges()`, `hasAtLeastRole()`, `normalize()` |

## Notes for Developers & AI

- **Backed Types:** All enums are backed by `string`. Use `->value` when storing in database tables or passing to external API contracts.
- **Strict Normalization:** Most enums provide a `normalize(mixed $value): ?self` static method designed to safely parse inputs regardless of case, accentuation, or whether the input is already an enum instance.
- **Labels vs Database Values:** `label()` methods leverage Laravel's translation helper `__('...')` to return localized human-readable labels according to the active application locale, keeping PHP identifiers completely in English.
- **Database Alignment:** `TicketPriorityEnum` and `TicketStatusEnum` use backing string values matching the legacy lookup schema in database tables (`tickets`, `ticket_statuses`). Do not alter their string values without a planned database schema and data migration.
